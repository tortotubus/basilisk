/**
# Array dimensions given by a function argument */

void func (int n, double b[n-1]) {
  double c = b[0];
  //  display_value (a);
}

int main()
{
  int nl = 2;
  { interpreter_verbosity (2);
    double a[nl];
    //    display_value (a);
    func (nl, a);
  }
}
