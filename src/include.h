void includes (int argc, char ** argv,
	       char ** grid, int * default_grid,
	       int * dimension, int * bg, int * layers, int * gpu,
               int * cuda, int * opencl, int * fp32,
	       const char * dir);
